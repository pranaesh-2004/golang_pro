services:
  - type: web
    name: flask-dashboard
    env: python
    buildCommand: ""
    startCommand: gunicorn -b 0.0.0.0:$PORT app:app
    envVars:
      - key: FLASK_ENV
        value: production
